query Tokens {
  tokens {
    id
    created_at
  }
}
mutation regenerateToken {
  delete_tokens(where: {}) {
    affected_rows
  }
  insert_tokens_one(object: {}) {
    id
  }
}

mutation deleteUser {
  delete_users(where: {}) {
    affected_rows
  }
}
